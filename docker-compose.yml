services:
  prediction-league:
    build: .
    ports:
      - "8000:8000"
    environment:
      - SPREADSHEET_ID=1oibdWWMrTXoFXozDIo4jfcukfNNJOfMbrTduzDS0Ji4
    volumes:
      - league-data:/app/data
    restart: unless-stopped

volumes:
  league-data:
